node_modules
.DS_Store
dist
dist-ssr
*.local
count.txt

# Environment files — never commit real credentials
.env
.env.*
!.env.example

# Native app locale reference files (local only, not part of the repo)
locales_native_app/

# Tooling caches
.astro
.nitro
.tanstack
.wrangler
